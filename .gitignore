# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node (only if you add tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build output (if added later)
dist/
build/

# Local Foundry config (per-developer, never commit)
foundryconfig.json

# Build artifacts produced by the release workflow
module.zip
